# ナビゲーションの仕様

ホーム、ドロワー、メニュー、ドラッグのあいだをどう行き来するかを定める。
既存のランチャーより使い勝手を高めることが目的であり、そのための原則を先に置き、各操作をその原則に照らして決める。
本書は #11 の作り直し(2026-09-03)を基準にし、#25 のジェスチャ整理(2026-09-05、他のランチャーの調査は docs/06)、#44〜#48 の編集モード(2026-09-12)を反映している。実装と食い違う点があれば本書を直す。

## 原則

ナビゲーションの良し悪しは、機能の数ではなく次の五つで決まる。

1. **指に追従する**。面の出入りは閾値で切り替えず、指の移動量に比例して動く。途中で離せば、その位置と速度から行き先が決まる。
2. **親指の届く場所に置く**。Fold8 のメイン画面は横長 4:3 で、片手で持つと上端に指が届かない。よく使う入口(検索、ドック)は下端に置く。
3. **状態の切り替えを減らす**。ドックはホームに固定し、ドロワーの一部にはしない。重ね描き(フォルダ、メニュー、設定)は同時に一つだけにする。
4. **同じ操作は同じ意味にする**。タップは開く、長押しはつまむ、上スワイプは一覧、下スワイプは検索。場所が変わっても意味を変えない。他のランチャーで広く使われている向きに合わせ、学び直しを要らなくする(#25、docs/06)。
5. **拒否を黙らせない**。置けない、開けない、起動できないときは、その場で分かる反応を返す。落とせないドロップは影が元の位置へ戻り、振動で拒否を伝える(#25)。

## 面の一覧

ランチャーは次の面を持つ。
ホームが土台で、他はその上に重なる。

| 面 | 何を出すか | 入り方 | 出方 |
| --- | --- | --- | --- |
| **ホーム** | ウィジェット面とアプリ面(カバーではめくる、開くと並ぶ)とドック | HOME キー、他の面を閉じる | なし(土台) |
| **ドロワー** | 検索欄、絞り込み、全アプリ | どの面でも上スワイプ(ドック、スクロールしないページ)、縦スクロール面の末尾で上に押す | 下にドラッグ、一覧の先頭で下に引く、Back、HOME キー |
| **検索(Finder)** | 端末の検索(Galaxy の Finder、無ければ全体検索)。どれも無ければドロワーを開いて検索欄に焦点 | どの面でも下スワイプ | 検索側で戻る、Back |
| **ウィジェット選択** | 自作ウィジェットと AppWidget の一覧 | 編集バーの WIDGETS | 外側をタップ、Back、ホームに置く |
| **フォルダ** | フォルダの中身と名前 | フォルダをタップ | 外側をタップ、Back、アプリを起動 |
| **アイテムメニュー** | ショートカット、アプリ情報、アンインストール、削除 | アイテムを長押しして動かさずに離す | 外側をタップ、項目を選ぶ、Back |
| **編集モード** | ✕ で消す、ウィジェットの大きさ、壁紙 / ウィジェット / 設定への入口 | 空き領域を長押し | DONE、Back、HOME キー |
| **設定** | テーマ選択(今後の設定項目もここ) | 編集バーの SETTINGS | CLOSE、Back |

ドロワーはアプリ一覧に絞り、ウィジェットの一覧は別の面(ウィジェット選択)に分けた(#25)。
他のランチャーと同じく、日常のアプリ探しとウィジェットの設置を混ぜないためである。
検索は自前で持たず、端末の全体検索(`SearchManager.globalSearchActivity`)へ渡す。Galaxy では Finder が開く。
全体検索が無い端末でだけ、ドロワーを開いて検索欄に焦点を当てる代替に落ちる。

編集モードは重ね描きではなく、ホームそのものの状態である。モード中もページはめくれ、ドロワーも開く。

重ね描き(フォルダ、アイテムメニュー、設定)は `Overlay` として一つだけ保持し、別の重ね描きに移るときは置き換える。
メニューの項目から別の面に移るときは、その項目がメニューを閉じない。
閉じてしまうと、移った先まで消えるからである(#1 で踏んだ不具合)。

ドロワーは重ね描きではなく、0 から 1 の進捗を持つ独立した層として扱う。
進捗が 0 より大きいあいだだけ合成され、進捗に応じて下から上がる。

## ホームのページ構成

ホームは **ウィジェット面** 1 枚と **アプリのページ** 何枚かで成り立つ(#19、#21、2026-09-04 決定)。
左端にウィジェットの流し読み面、右にめくれるアプリのページという、iOS と同じ並びである。
カバーではこれらを横にめくり、開くとウィジェット面が左、アプリのページが右に並ぶ。
カバーの幅がメインのちょうど半分という Fold8 の形を、そのまま画面の構造にした。

- ウィジェット面には、ウィジェットと大型フォルダを自由に置く。見る領域。画面に収まるあいだは固定で、はみ出すときだけ縦に伸びる。はみ出した段は下端で切れて見え、動かしているあいだは右端に細いスクロールバーが出る(#27)。末尾に空きを何段も残さない。残すと、下端で内容が切れず「ここで終わり」に見えてスクロールできると気づけない。
- アプリのページには、アプリとフォルダを自由に置く。触る領域。先頭のページが HOME の着地。
- アプリのページは段数が固定で、テーマの `grid.rows`(Fold8 は 7)で決まる。段数がそれより少ない画面でだけ縦に流す。
- どちらの面に何を置くかは縛らない。プリセットで誘導するだけにする。
- ページの増減は iOS 式。ドラッグ中に右端へ留めると次のページへめくれ、最後のページなら新しいページができる。空になったページはドラッグが終わると消える(最低 1 枚は残る)。

| 状態 | 見え方 |
| --- | --- |
| カバー(閉) | ウィジェット面、アプリ 1、アプリ 2、… を横にめくる。着地はアプリ 1。ウィジェット面は右へのスワイプ 1 回 |
| メイン横長(開) | 左にウィジェット面(縦スクロール)、右にアプリのページ(横めくり) |
| メイン縦長(開いて回転) | カバーと同じ並びで、余白を広げて置く |

開いたときの左右の境界は、`FoldingFeature` が縦向きのヒンジを返すならその位置と幅に合わせ、返さなければ半分にする。
開いているか閉じているかの判定そのものは幅で足りる(カバー 440〜475dp、メイン 816dp 以上)。ヒンジの情報は境界の補正にだけ使う。

「開いたときだけの内容」は持たない。
開く価値は、ウィジェット面とアプリのページが同時に見えることに置く。

この構造で、ページング(#8)はアプリのページとして実現され、メイン画面のドロワーの形(#9)はアプリ面の続きとして決まる。
並べ替え(#12)はページ内の詰め直しになる。

## ジェスチャの一覧

同じジェスチャはどの面でも同じ意味を持つ。

| ジェスチャ | 場所 | 結果 |
| --- | --- | --- |
| タップ | アプリ | 起動 |
| タップ | フォルダ | フォルダを開く |
| タップ | ショートカット | ショートカットを起動 |
| タップ | ウィジェットの中 | ウィジェット自身が処理(AppWidget のボタン等) |
| タップ | 重ね描きの外側 | 重ね描きを閉じる |
| 長押し(動かさない) | アイテム | アイテムメニュー |
| 長押し(動かさない) | ホームの空き領域 | 編集モードに入る |
| 長押し(動かさない) | ドロワーの項目 | 選択モードに入る(その項目を選ぶ) |
| 長押し → 移動 | アイテム、ドロワーの項目、メニューのショートカット | ドラッグ開始 |
| 触れて移動(長押し不要) | 編集モード中のアイテム | ドラッグ開始 |
| タップ | 編集モード中の ✕ | そのアイテムを消す |
| タップ | 編集モード中のウィジェット | 選んで角につまみを出す |
| 角のつまみを引く | 編集モード中の選んだウィジェット | 大きさを段単位で変える |
| タップ | 選択モード中のドロワーの項目 | 選択に出し入れ |
| 長押し → 移動 | 選択モード中のドロワーの項目 | 選んだものを束にして運ぶ |
| 縦スクロール | ホームの各ページ | ページを送る |
| 横にめくる | カバーのホーム、開いたときの右側 | ウィジェット面とアプリの各ページを切り替える |
| ドラッグ中に同じアプリの上に留める(0.5 秒) | グリッド、ドック | 輪が出て、離すとフォルダにまとまる |
| ドラッグ中に端へ留める | ページの左右端 | ページをめくる。最後のページの右端なら新しいページを作る |
| 上にスワイプ | ドック、スクロールしないアプリのページ | ドロワーを引き上げる(進捗に追従) |
| 下にスワイプ | ドック、スクロールしないアプリのページ | 検索(Finder)を開く |
| 末尾で上に押し続ける(抵抗 15% を超えて) | 縦スクロール面 | ドロワーを引き上げる |
| 先頭で下に引き続ける(抵抗 15% + 72dp) | 縦スクロール面 | 検索(Finder)を開く |
| 下にドラッグ | ドロワーの取っ手、一覧の先頭 | ドロワーを引き下げる |
| 縦スクロール | ドロワーの一覧 | 一覧を送る |
| Back | 重ね描きがある | 重ね描きを閉じる |
| Back | ドロワーが開いている | ドロワーを閉じる |
| Back | 編集モード | 編集を終える |
| HOME キー | どこでも | 重ね描きとドロワーをすべて閉じ、アプリの先頭ページに戻る |

## ドロワーの開閉

ドロワーは **進捗** という一つの値(0 = 閉、1 = 開)で動く。
入口は上スワイプに統一した(#25)。ドック、スクロールしないアプリのページ、縦スクロール面の末尾のいずれで上に払っても開く。
ドック右端のドロワーボタンは外した。上スワイプに一本化したことで、専用のボタンが要らなくなったためである。
上にドラッグすると、指の移動量を画面の高さで割った分だけ進捗が増える。
指を離したときの行き先は、まず速度で決め、速度が小さければ位置で決める。

縦方向の払いは、一度の指の動きの中でどちらに決まったかを覚え、決まった向きだけに流す。
上に動き始めたらドロワー、下に引いた量は検索の判定に積む。混ざると、少し上げてから下ろしたときに両方が動いてしまう。

**縦スクロールする面では抵抗を付ける**(#27)。
一覧が消費しきれない端の余りを受けるが、端に当たってすぐには動かない。
画面の高さの 15% を引き続けるまでは一覧が伸びる(標準の overscroll)だけで、それを超えた分からドロワーの進捗に付いてくる。
先頭で下に引く検索も同じで、抵抗の 15% に加えてさらに 72dp 引いてから離すと開く。
見たくてスクロールしているのに面が切り替わる、という取り違えを防ぐためである。
指の動き以外(フリングの減速で端に当たった分)は受けない。受けると、指を離したあとに勝手に面が動く。

抵抗を越えたら、その向きに決まったものとして扱う。
逆向きに強く払い戻さない限り、指を離した位置がどこであれ開く(閉じるときは閉じる)。
位置の半分で決めると、抵抗のぶんに加えて画面の半分まで引かないと開かず、指を離した瞬間に戻ってしまう。
ただし、越えたあとに元の位置まで戻しきったら「決まった向き」は解く。
解かないと、戻しきって離しても確定してしまい、戻す向きの指の動きも一覧に返らない。

- 上向きに 500 px/秒 より速い → 開く
- 下向きに 500 px/秒 より速い → 閉じる
- それ以外 → 進捗が 0.5 を超えていれば開く、超えていなければ閉じる

行き先が決まったら、ばねの動き(跳ね返りなし)で 0 か 1 に落ち着く。

開くあいだ、ホームは奥に引く。
全開で 4% 縮み、透明度は 30% まで下がる。
ドロワーがホームの上に乗っているのではなく、ホームが下がってドロワーが出てくる、という見え方にする。

閉じる操作は三つある。

1. ドロワー上端の取っ手を下にドラッグする。進捗に追従し、離せば同じ規則で行き先が決まる。
2. 一覧を先頭まで戻した状態でさらに下に引く。一覧が消費しきれなかった分をドロワーの進捗に回す。一覧が動けるあいだは一覧が優先されるので、スクロールと閉じる操作が競合しない。
   ただし先頭に当たってすぐには閉じない(#27)。画面の高さの 15% は一覧が伸びるだけで、それを超えて引き続けたときだけ進捗が付いてくる。2500 px/秒 より速く払ったときは距離に関わらず閉じる。
   一覧を勢いよく先頭まで戻したときに、フリングの残りで閉じてしまわないようにするため。
3. Back。

ドロワーからアプリを起動したとき、あるいはドロワーから何かをホームに落としたときは、ドロワーを閉じる。

### 指の移動を取りこぼさない

進捗の更新はアニメーション用の値に非同期で反映するが、指の移動そのものは同期的に積む。
非同期の値から次の値を計算すると、1 フレームに複数回来る移動を取りこぼし、大きく引いても少ししか開かない(作り直しの途中で踏んだ)。
速度は自前で測らず、Compose の `draggable` が返すものを使う。

## ドロワーの中身

上から順に、取っ手、検索欄、絞り込みチップ、一覧の四段にする(#29)。

**取っ手** は 28dp の帯で、下にドラッグして閉じるためにある。

**検索欄** は上に置く。
当初は原則 2(親指の届く場所)から下端に置いていたが、上からスワイプで入る面なら指はもう上にある。
探す動作の入口と欄を同じ側に揃えるほうが迷いがない。

**絞り込みチップ** は一つだけ選べ、もう一度押すと外れる。並ぶのは次のもの。

| チップ | 中身 | 並び |
| --- | --- | --- |
| RECENT | 直近 7 日に起動したアプリ | 最終起動の新しい順 |
| FREQUENT | 直近 7 日に起動したアプリ | 起動回数の多い順 |
| NEW | 14 日以内に入れたアプリ | 入れた順(新しいものが先) |
| GAME、AUDIO、VIDEO、IMAGE、SOCIAL、NEWS、MAPS、PRODUCTIVITY | manifest の `appCategory` が一致するアプリ | 名前順 |

カテゴリのチップは、付けているアプリが 1 つでもあるものだけ出す。付けていないアプリは多く、どのカテゴリにも出ない。
RECENT と FREQUENT は使用状況の権限が要る。未許可なら一覧の上に許可画面への導線を出す。
規則つきフォルダ(最近、よく使う、カテゴリ)はこの絞り込みに置き換えて廃止した(docs/04)。

**一覧** はアプリのグリッドだけにする(#25)。列数はホームと同じ(カバーと縦長で 6 列、横長で 12 列)。
検索語と絞り込みは重ねて効く。ウィジェットの一覧は編集バーの WIDGETS から別の面で開く。

## 検索

検索には二つある。端末の検索と、ドロワーの中でアプリだけを絞る欄である。

**端末の検索(Finder)** は、どの面でも下スワイプで開く。
当初は `SearchManager.globalSearchActivity` に丸投げしていたが、Fold8 では Galaxy の Finder ではなく Google アプリが開き、そこから他のことができなかった(#29)。
Finder は全体検索として登録されているとは限らないので、次の順で探す。

1. 全体検索を扱う活動のうち、Finder のもの
2. Finder のパッケージの起動 Intent
3. 端末が選んでいる全体検索(素の Android では Google)

どれも無ければ、ドロワーを開いて検索欄に焦点を当てる。
どの経路で開いたかは実機でしか分からないので、ログに残す。自前の検索を強くしてこちらを主にするかは、実機で Finder の使い勝手を見てから決める。

**ドロワーの検索欄** はタップで入力状態になる。
入力のたびに一覧が絞り込まれ、先頭に戻る。

絞り込みはアプリ名とパッケージ名を対象にし、次の順位で並べる。

1. アプリ名が入力で始まる
2. アプリ名の単語のどれかが入力で始まる
3. アプリ名が入力を含む
4. パッケージ名が入力を含む

同じ順位の中ではアプリ名の順にする。
先頭一致を上に出すのは、確定キーで先頭を起動したときに狙ったものが出るようにするためである。
確定キーは Go にし、先頭の結果を起動してドロワーを閉じる。
入力があるときは CLEAR を出し、タップで空にする。

タップで入力状態にならない事象が AVD で出ており、実機での確認待ちである(#14)。

## 開くときの動き

開くものは、触った場所から広がる(#23)。
どこを触ったらどれが開いたのかが見えないと、画面が飛んだように感じるためである。

**重ね描き**(フォルダ、二つのメニュー、設定)は、触った要素の矩形を受け取り、その中心を拡大の原点にする。
0.86 倍から等倍へ、透明から不透明へ、220 ミリ秒かけて広がる。閉じるときは 160 ミリ秒で逆に縮む。
閉じる動きを見せるため、状態が空になったあとも終わるまで描き続ける。
拡大の原点を測る矩形は、変形をかける層より外側で測る。内側で測ると、広がる途中で原点が動いてしまう。

**アプリとショートカットの起動** には `ActivityOptions.makeScaleUpAnimation` を渡し、アイコンの矩形から画面が広がるようにする。
あわせて `sourceBounds` も渡す。アプリ側がメニューの位置決めに使うことがある。
矩形が分からないとき(検索の確定キーで起動したときなど)は何も渡さず、システム既定の遷移に任せる。

触った要素の矩形は、タップを扱うジェスチャ処理が位置情報を持っているので、そこからタップの引数として渡す。
描画側が別途座標を測る必要はない。

## 長押しの切り分け

長押しは「つまむ」の意味で統一するが、何をつまむかで行き先が違う。
同じ指の動きなので、取り合いが起きないように判定の順番を決めておく。

**アイテムの上** では、ひとつのジェスチャ処理でタップと長押しを順に判定する。

1. down を受ける
2. 長押しの時間(端末設定、およそ 400〜500 ミリ秒)まで待つ。そのあいだに指が離れればタップ、動きすぎればスクロール等に譲る
3. 長押しが成立したら振動し、ドラッグを始める
4. 離すまでの移動が 12dp 未満ならアイテムメニュー、以上ならドロップ

タップ検出と長押し検出を別々のコルーチンで並べると、片方が最初の down を消費してもう片方に届かず、タップが一度も成立しない(作り直しで見つけた不具合)。
順に判定する形にしてある。

**ホームの空き領域** では、アイテムの長押しより 150 ミリ秒だけ長く待ってから、ドラッグが始まっていないことを確かめて編集モードに入る。
アイテムの上で長押しすれば、この時点でドラッグが始まっているので、編集モードには入らない。
待ち時間で切り分けるのは、Compose のポインタ処理では親と子の両方に同じ長押しが届くためである。

**AppWidget の上** では Compose のポインタ処理が使えないので、ホストビューの派生クラスで長押しを横取りし、画面座標でドラッグ制御に渡す。
横取りしたら親の横取りを禁止し、Compose 側のスワイプ検出に奪われて CANCEL が来るのを防ぐ。

## アイテムメニュー

長押しして動かさずに離すと出る。
触ったアイテムを起点にした吹き出しで、上に収まらなければ下に出す(#45)。指の下でレイアウトが動かないように、帯やページ送りは伴わない。
アイテムの種類で項目が変わる。

| 種類 | 項目 |
| --- | --- |
| アプリ | そのアプリのショートカット(あれば)、APP INFO、UNINSTALL、REMOVE |
| フォルダ | OPEN / RENAME、REMOVE |
| 自作ウィジェット、AppWidget | SIZE(いまの大きさ。変えるのは編集モード)、REMOVE |
| ショートカット | LAUNCH、REMOVE |

REMOVE はレイアウト上のアイテムにだけ出す。
ドロワーでは長押しが選択モードの入口なので、メニュー自体を出さない(#48)。

ショートカットは Android のアプリショートカット(`LauncherApps.getShortcuts`)で、宣言済み、動的、固定済みのすべてを順位順に並べる。
既定ホームでないときは読めないので、項目自体が出ない。
タップで起動し、長押しドラッグでホームに固定する。
固定するとき、既に固定済みのものと合わせて `pinShortcuts` に渡す(与えた集合で置き換わるため)。

## 編集モード

空き領域の長押しで入る(#46)。iOS と同じ作りにした。

- アイテムがわずかに揺れ(テーマで切れる)、左上に ✕ が出る。✕ で消える
- 長押しを待たずに、触れてずらせばつまめる
- ウィジェットはタップで選ばれ、右上・左下・右下に丸いつまみが出る(#47)。左上は ✕ の場所なので出さない
- つまみを引くと、引いた辺が指に付いてくる。上や左の辺を引くと反対の角を留めて原点が動く。段をまたぐたびに 1 段ずつ変わり、置けない大きさなら振動で返して変わらない
- 下端の編集バーに WALLPAPER / WIDGETS / SETTINGS / DONE を並べる。壁紙、ウィジェット、設定の三つは Pixel でも One UI でも長押しメニューの最小構成で、それに揃えた
- DONE、Back、HOME キーで終える。設定やウィジェット選択へ移ってもモードは続く

アプリの追加はドロワー(上スワイプ)から長押しドラッグで行うので、ここには置かない(#25)。

✕ とつまみは、見た目の丸を小さく保ったまま、当たり判定を 44dp 取る(#32、#53)。
当たり判定はセルの内側へ広げる。外へ広げるとページの縁で切られ、端の列で当たらなくなる。

ドロワーでは、項目の長押しで**選択モード**に入る(#48)。
選んだ項目に ✓ が付き、上部に「N SELECTED // DRAG TO HOME」の帯と CLEAR が出る。
タップで出し入れし、どれかを長押しドラッグすると選んだものが束になって運ばれ、落とした場所から空きセルへ順に置かれる。収まらない分は次のページへ流す。
ドロワーを閉じると選択は解ける。

## ウィジェット選択

編集バーの WIDGETS で開く。
自作ウィジェットと、端末の AppWidget プロバイダを並べる。
それぞれのカードを長押しドラッグしてホームに置く。AppWidget は最小サイズをセル数に切り上げて渡す。
アプリ一覧とは分ける。日常のアプリ探しと、たまにしかしないウィジェットの設置を混ぜないためである(docs/06)。

## ドラッグとドロップ

ドラッグは長押しから始まり、64dp の影が指に追従する。
影はアイコンがあればアイコン、なければ名前を書いた枠にする。

**出発点** は次のどれか。

- ホームのグリッド、ドック、フォルダの中(レイアウト上の位置を持つ)
- ドロワーの APPS(レイアウト外。落とすと新しく置かれる。選択モードなら束で)、ウィジェット選択
- アイテムメニューのショートカット(レイアウト外。落とすと固定される)

**落とし先** は次のどれか。

- ウィジェット面のグリッド、アプリの各ページのグリッド
- ドック

消すのはドロップではなく、アイテムメニューの REMOVE か編集モードの ✕ で行う(#44)。
かつては画面上端に削除帯を出していたが、帯のぶんホームが下がって長押しした指の下でアイテムがずれるので止めた。

グリッドに落とすときの規則は次のとおり(#25 で押しのけと留めを入れた)。

- 空きセルなら置く。ウィジェットは幅と高さの分だけ空いている必要がある
- 占有セルに落とすと、相手を最も近い空きへ押しのけて割り込む(reflow)。近いものから順に動かし、どれも動かせなければ拒否する
- フォルダにまとめるのは、同じアプリの上に **0.5 秒留めた** ときだけにする。留めると輪が出て、離すとフォルダになる。手動フォルダの上に留めれば中に入る
- 通り過ぎるだけではフォルダにしない。並べ替えの途中でアイコンの上を通っても、勝手にまとまらないようにするため
- 置けないときは拒否し、影が元の位置へ戻って振動する

**落とす位置の事前表示**。ドラッグ中、落ちるセルに枠を出す。何が起きるかで描き分ける。

| 種類 | 見え方 |
| --- | --- |
| 置く(空き) | アクセント色の角丸枠 |
| 押しのけ | アクセント色の角丸枠(内側を薄く塗る) |
| まとめ(留め成立) | アクセント色の輪 |
| 拒否 | 赤い枠 |

ドックにはアプリとフォルダだけ置ける。留めればドックの上でもフォルダにまとまる。

**1 個になったフォルダは残す**(#25)。フォルダから最後の 1 個を残して出しても、フォルダの枠は消さない。
中身が 0 になったときだけフォルダを消す。
数が減るたびに枠が出たり消えたりすると、まとめ直すのが面倒になるためである。

ドラッグ中、出発点の重ね描き(ドロワー、フォルダ、メニュー)は見えなくなるが合成には残す。
ジェスチャを続けるためである。
受け付けたら重ね描きを閉じる。拒否されたときは閉じず、続けて置き直せるようにする。

## 到達距離の目標

原則 3 を数で言い直す。
どの操作も、ホームから次の回数で届くことを目標にする。

| したいこと | 操作 | 回数 |
| --- | --- | --- |
| ドックのアプリを起動 | タップ | 1 |
| ホームに置いたアプリを起動 | タップ | 1 |
| フォルダの中のアプリを起動 | タップ、タップ | 2 |
| 置いていないアプリを起動 | 上スワイプ、タップ | 2 |
| 名前で探して起動(端末の検索) | 下スワイプ、入力、確定 | 3 |
| 最近使ったアプリを起動 | 上スワイプ、RECENT、タップ | 3 |
| アプリのショートカットを起動 | 長押し、タップ | 2 |
| ホームのアイテムを消す | 長押し、REMOVE | 2 |
| ホームのアイテムをまとめて消す | 空き領域を長押し、✕ を続けて押す | 1 + 個数 |
| ウィジェットの大きさを変える | 空き領域を長押し、タップ、つまみを引く | 3 |
| 複数のアプリをホームに置く | 上スワイプ、長押し、タップ×N、長押しドラッグ | 3 + N |
| アプリをホームに置く | 上スワイプ、長押しドラッグ | 2 |
| ウィジェットをホームに置く | 空き領域を長押し、WIDGETS、長押しドラッグ | 3 |
| ウィジェット面を見る(カバー) | 横スワイプ | 1 |
| 2 ページ目のアプリを起動(カバー) | 横スワイプ、タップ | 2 |
| テーマを変える | 空き領域を長押し、SETTINGS、タップ | 3 |

## カバー画面とメイン画面の違い

ジェスチャと面の構造は両画面で同じにし、違いはページの見せ方だけにする。

- カバー: ウィジェット面とアプリの各ページを 1 枚ずつ横にめくる。6 列
- メイン横長: 左にウィジェット面、右にアプリのページ。各 6 列。境界はヒンジの位置
- メイン縦長: カバーと同じ並びで、余白を広げる。6 列
- ドロワーの列数: カバーと縦長で 6、横長で 12
- 検索欄: どれも下端

左右どちらにウィジェット面を置くか(#3)は決めても、ジェスチャと入口の位置は変えない。

## 未決と今後

- 検索欄の入力状態(#14)。全体検索を持つ端末では下スワイプで足りるので、代替の欄でだけ関わる
- アイコンとグリッドの調整を設定から(#13)
- ドロワーの索引(頭文字で飛ぶ)。アプリが増えたときに検索の代わりになる
- 削除の取り消し。誤って消したあと数秒だけ戻せるようにする
- 実機での確認待ち: 下スワイプが Galaxy の Finder を開くか、上スワイプとページ送りの切り分け、留めの 0.5 秒が長すぎないか

## タスクバー(実験中、#55〜#57)

One UI は開いた画面で他のアプリを使っているあいだ、下端にドックと最近のアプリを出し、ドラッグで分割起動できる。
これは既定ホームの特権機能(Launcher3 の QuickStep 連携)で、サードパーティのランチャーには公開されていない。
うちが既定ホームになるとタスクバーが消えるのはこのためである。

代わりに、ユーザー補助サービスとオーバーレイで同じ働きを作る(farmerbb の Taskbar と同じ方式)。段階ごとに実機で確かめて進める。

| 段 | 何をするか | 使うもの |
| --- | --- | --- |
| #55 | 前面のアプリと画面の開閉を知る | `AccessibilityService`(`TYPE_WINDOW_STATE_CHANGED`。画面の内容は読まない)、最大ウィンドウの最小幅 |
| #56 | 開いた画面で他アプリが前面のときだけ、下端にドックを重ねる | `TYPE_ACCESSIBILITY_OVERLAY` |
| #57 | 2 つ選んで分割起動 | `GLOBAL_ACTION_TOGGLE_SPLIT_SCREEN` + `FLAG_ACTIVITY_LAUNCH_ADJACENT` |

できないこと: 3 分割、Samsung のアプリペア、左右どちらに置くかの指定、最近のアプリのサムネイル。

開いた画面の判定は最大ウィンドウの最小幅が 600dp 以上かで行う(Fold8 はカバー 512dp、開くと 758dp)。
サービスの状態と直近に前面へ出たアプリは、設定の TASKBAR 節と METRICS ウィジェットに出す。
